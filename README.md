# node-ms-template
A Node JS template that has deploy code, testing and firebase integration already setup

# Steps to build and run the container

1. docker build -t braidenjudd/node-ms-template .
2. docker run -p 49161:8081 -d braidenjudd/node-ms-template